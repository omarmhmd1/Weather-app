# Weather App
That application can display the weather, It depends on sending requests to the API and get data, then, display it on the website
This app is developed by using Django framework

## Requirements
``` bash
pip install django
```
It is the main requirement
``` bash
pip install requests
```
for sending requests to the API
